# Simple static frontend build without npm dependencies
FROM nginx:alpine

# Copy static assets
COPY static-dist/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 5173

CMD ["nginx", "-g", "daemon off;"]